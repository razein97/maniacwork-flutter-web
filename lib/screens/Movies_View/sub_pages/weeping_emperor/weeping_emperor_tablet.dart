import 'package:flutter/material.dart';

class WeepingEmperorTablet extends StatelessWidget {
  const WeepingEmperorTablet({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
